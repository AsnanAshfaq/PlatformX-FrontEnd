import React, { FC } from "react";
import { Posts } from "../assets/Constant/sample.js";
import Card from "./Card";

const CenterPanel: FC = () => {
  return (
    <div>
      {/* {Posts.map((item) => (
        <Card key={item.id} date={item.date} text={item.text} />
      ))} */}
    </div>
  );
};
